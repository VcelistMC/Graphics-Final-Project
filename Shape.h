#pragma once
#include <vector>
/*#include <windef.h>

class Shape
{
	std::vector<POINT> points;
public:
	void setPoints(std::vector<POINT> points) {
		this->points = points;
	}

	virtual int getMinNumOfPoints() = 0;
	virtual void draw() = 0;
};

*/